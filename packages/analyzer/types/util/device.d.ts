import { Log } from '../analyzer';
export declare const handleDeviceInfo: (sessionMap: Map<string, Log[]>) => {
    screenMap: Map<string, number>;
    clientMap: Map<string, number>;
    browserMap: Map<string, number>;
};
